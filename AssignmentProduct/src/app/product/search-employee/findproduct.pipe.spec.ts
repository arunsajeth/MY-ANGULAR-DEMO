import { FindproductPipe } from './findproduct.pipe';

describe('FindproductPipe', () => {
  it('create an instance', () => {
    const pipe = new FindproductPipe();
    expect(pipe).toBeTruthy();
  });
});
